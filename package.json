{
  "name": "back-appsync-boilerplate",
  "version": "1.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "yarn rimraf dist",
    "test": "jest",
    "cdk": "cdk",
    "deploy": "yarn clean && yarn build && yarn cdk deploy",
    "destroy": "yarn cdk destroy",
    "synth": "yarn cdk synth > template.yaml"
  },
  "dependencies": {
    "aws-cdk": "^2.98.0",
    "aws-cdk-lib": "^2.98.0",
    "aws-sdk": "^2.1465.0",
    "constructs": "^10.2.70",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "@types/node": "^20.7.0",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
